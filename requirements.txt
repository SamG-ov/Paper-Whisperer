# PaperWhisperer dependencies (direct dependencies only, exact-pinned).
#
# This is a MANIFEST (curated direct deps), not a lockfile. Do NOT overwrite
# it with `pip freeze`. If you want a full lockfile, write a SEPARATE file
# (e.g. pip freeze > requirements.lock).
#
# Install:  py -m pip install -r requirements.txt   (use Python 3.12)

# --- Orchestration framework (version-locked family) ---
langchain==0.3.30
langchain-community==0.3.31

# --- LLM + embeddings provider (Google Gemini) ---
langchain-google-genai==2.1.12

# --- Vector database (1.x ships prebuilt wheels) ---
langchain-chroma==0.2.6
chromadb==1.5.9

# --- Hybrid (keyword) retrieval ---
rank-bm25==0.2.2

# --- PDF parsing ---
pypdf==6.13.0

# --- Web UI ---
streamlit==1.58.0

# --- Config / secrets loading ---
python-dotenv==1.2.2
pydantic-settings==2.14.1

# --- TLS: use the OS trust store (handles AV/proxy HTTPS interception) ---
truststore==0.10.4
